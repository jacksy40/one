<div class="one-img-container">
  <div class="img menu-img">
      <%= image_tag('menu.jpg') %>
  </div>
</div>

<div class="menu-one-container">
  <div class="menu-bar">
  <a id="appetizer">Appetizer</a><a id="soups">Soups</a><a id="dumplings">Dumplings</a><a id="rice">Rice</a><a id="lo-mein">Lo-Mein</a><a id="beef">Beef</a>
  <a id="chicken">Chicken</a><a id="pork">Pork</a><a id="seafood">Seafood</a><a id="drinks">Drinks</a>
  </div>
</div>

<div class="menu-two-container">
  <div class="panel small-9 small-centered columns appetizer-container">
  <h3>Appetizer</h3>
  <% @appitizer.each do |m| %>
    <ul class="item">
     <li><strong><%= link_to m.name, menu_path(m.id) %></strong></li>
     <li>$ <%= m.price %></li>
     <div><%= image_tag(m.picture.url) %></div>
    </ul>
  <% end %>
  </div>
  <div class="panel small-9 small-centered columns rice-container">
  <h3>Rice</h3>
   <% @rice.each do |m| %>
    <ul class="item">
     <li><strong><%= link_to m.name, menu_path(m.id) %></strong></li>
     <li>$ <%= m.price %></li>
     <li><%= image_tag(m.picture) %></li>
    </ul>
  <% end %>
  </div>
  <div class="panel small-9 small-centered columns lo-mein-container">
  <h3>Lo-Mein</h3>
  <% @lo_mein.each do |m| %>
    <ul class="item">
     <li><strong><%= link_to m.name, menu_path(m.id) %></strong></li>
     <li>$ <%= m.price %></li>
     <li><%= image_tag(m.picture) %></li>
    </ul>
  <% end %>
  </div>
  <div class="panel small-9 small-centered columns dumplings-container">
  <h3>Dumplings</h3>
  <% @dumplings.each do |m| %>
    <ul class="item">
     <li><strong><%= link_to m.name, menu_path(m.id) %></strong></li>
     <li>$ <%= m.price %></li>
     <li><%= image_tag(m.picture) %></li>
    </ul>
  <% end %>
  </div>
  <div class="panel small-9 small-centered columns beef-container">
  <h3>Beef</h3>
  <% @beef.each do |m| %>
    <ul class="item">
     <li><strong><%= link_to m.name, menu_path(m.id) %></strong></li>
     <li>$ <%= m.price %></li>
     <li><%= image_tag(m.picture) %></li>
    </ul>
  <% end %>
  </div>
  <div class="panel small-9 small-centered columns chicken-container">
  <h3>Chicken</h3>
  <% @chicken.each do |m| %>
    <ul class="item">
     <li><strong><%= link_to m.name, menu_path(m.id) %></strong></li>
     <li>$ <%= m.price %></li>
     <li><%= image_tag(m.picture) %></li>
    </ul>
  <% end %>
  </div>
  <div class="panel small-9 small-centered columns pork-container">
  <h3>Pork</h3>
  <% @pork.each do |m| %>
    <ul class="item">
     <li><strong><%= link_to m.name, menu_path(m.id) %></strong></li>
     <li>$ <%= m.price %></li>
     <li><%= image_tag(m.picture) %></li>
    </ul>
  <% end %>
  </div>
  <div class="panel small-9 small-centered columns seafood-container">
  <h3>Seafood</h3>
  <% @seafood.each do |m| %>
   <ul class="item">
     <li><strong><%= link_to m.name, menu_path(m.id) %></strong></li>
     <li>$ <%= m.price %></li>
     <li><%= image_tag(m.picture) %></li>
    </ul>
  <% end %>
  </div>
  <div class="panel small-9 small-centered columns soups-container">
  <h3>Soups</h3>
  <% @soups.each do |m| %>
    <ul class="item">
     <li><strong><%= link_to m.name, menu_path(m.id) %></strong></li>
     <li>$ <%= m.price %></li>
     <li><%= image_tag(m.picture) %></li>
    </ul>
  <% end %>
  </div>
  <div class="panel small-9 small-centered columns drinks-container">
  <h3>Drinks</h3>
  <% @drinks.each do |m| %>
    <ul class="item">
     <li><strong><%= link_to m.name, menu_path(m.id) %></strong></li>
     <li>$ <%= m.price %></li>
     <li><%= image_tag(m.picture) %></li>
    </ul>
  <% end %>
  </div>
</div>

<div class="menu-three-container">
<% if @menu %>
  <% @menu.each do |image| %>
    <%= image_tag(m.picture) %>
  <% end %>
<% end %>
</div>
